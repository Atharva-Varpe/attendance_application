services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - backend-data:/data
    environment:
      - SECRET_KEY=your-production-secret
    networks:
      - app-network

volumes:
  backend-data:

networks:
  app-network:
    driver: bridge